int main(){
    unsigned char i= -130; //unsigned:values from 0 to 255.
    //so its a infinite loop  as the values will keep flowing between 0 to 255
    for(;i>=0;i++);
        printf("%d\n",i);
        return 0;
    }
